import {e2eComponentPage} from './e2e-cmp.po';
/*
describe('e2e testing component', () => {
  // set up our page
  let page: e2eComponentPage;

  beforeEach(() => {
    page = new e2eComponentPage();
  });

  // actual tests
  it('should display the title Example e2e testing component', () => {
    page.navigateTo();
    expect(page.getTitle()).toEqual('Example e2e testing component');
  });

  it('should start with 1 point', () => {
    page.navigateTo();

    expect(page.getPoints()).toEqual('1');

    // simulate clicks on Plus1 button
    page.getPlus1Button().click();
    expect(page.getPoints()).toEqual('2');

    page.getPlus1Button().click();
    page.getPlus1Button().click();
    page.getPlus1Button().click();
    expect(page.getPoints()).toEqual('5');

  });

  it('should reset points by clicking Reset', () => {
    page.navigateTo();

    page.getPlus1Button().click();
    page.getPlus1Button().click();
    page.getPlus1Button().click();

    expect(page.getPoints()).toEqual('4');

    page.getResetButton().click();

    expect(page.getPoints()).toEqual('0');
  });
});
*/
